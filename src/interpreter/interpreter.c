#include<stdio.h>
#include "interpreter.h"

void modoBatch(char* caminhoFicheiros, char* queries)
{
    printf("Modo batch \n");
    printf("%s, %s \n", caminhoFicheiros, queries);
}

void modoInterativo()
{
    printf("Modo interativo \n");
    // Criar um menu interativo
    // do
    // {
        
        
    // } while ();
    
}
